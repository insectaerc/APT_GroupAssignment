complile command:
g++ main.cpp member.cpp guest.cpp admin.cpp
if ($?) { g++ main.cpp member.cpp -o main } ; if ($?) { .\main }

class
  Customer
    
	Member
		attribute
      bool isMember
      int rating
      int creditPoints = 500
			string username
			string password
			string fullname
			string phonenumber
      string review
      House myHouse
      House ocuppyingHouse = NULL
		function
      showMemberInfo()
      //Quan
      list(House myHouse){
        consumingPoint
        check occupierRatingRequired
      }
      unlist()
      requestOccupy(House house)

      //Hoang
      viewHouses(){
        for(House house: vectorOfHouses){
          ->all info 
        }
      }
      searchHouses(string cityName)
      acceptRequest(Member occupier)
        this.creditPoints +
        occupier.creditPoints -
        this.myHouse.availability = false;
      temp returnHouse

  //Duy Hoang
  Guest
    function
      viewHouses(){
        for(House house: vectorOfHouses){
          ->all info but not reviews and availability
        }
      }
      register(fullname, username, password, phonenumber, House myHouse){
        save info in file
      }
        
    
  //Chi Quyen
	Admin
		Atrtribute
			String username = admin123
			String password = admin123
		function
      viewMemberInfo()
      viewHouseInfo()
			
	House
    atribute
      string review
      int rating
      availability
      Member owner
		  location
		  description
